import { StyleSheet } from "react-native";

export const appTheme = StyleSheet.create({
  container: {
    alignContent: 'center',
    alignItems: 'center',
    flex: 1,
    justifyContent: 'center'
  },
  text: {
    color: 'violet',
    fontSize: 40,
    fontWeight: 'bold',
    textAlign: 'center'
  }
});
